"""
Entry point for the calculator application.

It initializes and runs the calculator app.
"""

from app.calculator import calculator

# Ensure the calculator runs only when this script is executed directly.
if __name__ == "__main__":

    calculator()
